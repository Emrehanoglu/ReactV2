const authState = {} /* başlangıç değeri */

const authReducer = (state = authState, action) => {
    switch (action.type){
        default:
            return state
    }
}

export default authReducer